services:
  db:
    image: postgres:17-alpine
    container_name: dev_board
    restart: no
    ports:
      - "5432:5432"
    env_file:
      - .env
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - dev_board_pgdata:/var/lib/postgresql/data

volumes:
  dev_board_pgdata:
